version: "2"
sql:
  - engine: postgresql
    schema: migrations
    queries: sql/queries
    gen:
      go:
        package: dbgen
        out: internal/database/dbgen
        sql_package: pgx/v5
        emit_json_tags: true
        emit_interface: true
